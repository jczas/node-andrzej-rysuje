pre 1. Install node.js
pre 2. Install express
% npm install -g express-generator

pre 3. Install 
%n npm install -g nodemon

1. Prepare new project in console:
% express node-andrzej-rysuje --view=pug

2. Install modules
% npm install

3. Start node.js using nodemon
% nodemon ./bin/www

3.1. Open site in browser http://127.0.0.1:3000/

3.2 How nodemon works

3.3. Debuging

3.4 Debuging with nodemon https://code.visualstudio.com/docs/editor/node-debugging
3.4.1 Attache
---
        {
            "type": "node",
            "request": "attach",
            "restart": true,
            "name": "Attach to Process",
            "port": 5858
        }
---
3.4.2 Lunch
---
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceRoot}\\bin\\www",
            "cwd": "${workspaceRoot}",
            "runtimeExecutable": "nodemon",
            "runtimeArgs": [
                "--debug=5858"
            ],
            "restart": true,
            "port": 5858,
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        },
---

4. Add repository on github: https://github.com/
4.1. Create .gitignore: https://github.com/github/gitignore
% echo "# node-andrzej-rysuje" >> README.md
% git init

4.2. Add to repository, commit and push
% git add *
% git ci -m "init version"
% git remote add origin https://github.com/jczas/node-andrzej-rysuje.git
% git push -u origin master

5. Azure
5.1 login
> azure login
> azure config mode arm
> azure group create node-andrzej-rysuje-rg --location 'North Europe'
> azure appserviceplan create --resource-group node-andrzej-rysuje-rg --name node-andrzej-rysuje-sp --location 'North Europe' --sku F1
> azure webapp create --resource-group node-andrzej-rysuje-rg --plan node-andrzej-rysuje-sp --name node-andrzej-rysuje --location 'North Europe'


4. Install winston modul (loggin)
% npm install winston --save 



5. Install xmldom modul
% npm install xmldom --save

6. Install xpath modul
% npm install xpath --save

7. Install request modul
% npm install request --save

8. Request rss from: http://www.andrzejrysuje.pl/wordpress/feed/
8.1. Import modul
---
var request = require('request');
---

8.2. Request rss
--- 
  var url = 'http://www.andrzejrysuje.pl/wordpress/feed/';

  request(url, function (error, response, xml) {
    if (!error) {
      res.send(xml);
    } else {
      res.status(404);
      res.send('Problem with ' + url);
    }
  });
---

9. Get img.src
9.1. Import modules
---
var xpath = require('xpath');
var dom = require('xmldom').DOMParser;
---

9.2. Find nodes (encoded)
---
      var doc = new dom().parseFromString(xml);
      var select = xpath.useNamespaces({ 'content': 'http://purl.org/rss/1.0/modules/content/' });
      var nodes = select('//content:encoded', doc);
      console.log('nodes.length: ' + nodes.length);
---

10. Take img and alt with regex
---
      var imgEx = /src="([^"]*)"/;
      var altEx = /alt="([^"]*)"/;

      for (var i = 0, len = nodes.length; i < len; i++) {
        var data = nodes[i].firstChild.data;

        var altMatch = altEx.exec(data);
        var imgMatch = imgEx.exec(data);
        if (imgMatch !== null && altMatch !== null) {
          console.log('img: ' + imgMatch[1]);
          console.log('alt: ' + altMatch[1]);
        }      }
---

11. Install rss modul
% npm install rss --save

12. Generate rss feed
12.1. Import modul
---
var RSS = require('rss');
---

12.2. Create rss feed 
---
      var feed = new RSS({
        title: 'Andrzej Rysuje',
        site_url: 'http://www.andrzejrysuje.pl',
        image_url: 'http://www.andrzejrysuje.pl/assets/img/logo.png',
        ttl: '5'
      });
---

12.3. Return rss feed
---
                    res.set('Content-Type', 'text/xml');
                    res.send(feed.xml());
---
12.4. Add item
---
          feed.item({
            title: altMatch,
            url: imgMatch
          });
---



